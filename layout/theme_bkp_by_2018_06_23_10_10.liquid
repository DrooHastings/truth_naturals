<!doctype html>
<!--[if IE 9]> <html class="ie9 no-js"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html class="no-js"> <!--<![endif]-->

<head>
  {% if handle contains '2x-1500mg-cbd-oil-tincture-peppermint-natural-lemon-flavours-30ml' %}

{% endif %}
<meta name="google-site-verification" content="WfFdkxBFGm3tFHeEFYUj9f1pfnkSOFsdqr-gJJtZB68" />
  <!-- Blockshop Theme - Version 4.0.6 by Troop Themes - http://troopthemes.com/ -->
  {% include 'framework--version' %}

  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<meta content="origin" id="mref" name="referrer">

  {% if settings.favicon != blank %}
    <link rel="shortcut icon" href="{{ settings.favicon | img_url: '32x32' }}" type="image/png">
  {% endif %}

  {% if template == 'index' %}
    <title>{{ page_title }}</title>
  {% elsif template == 'password' %}
    <title>{{ shop.name }}</title>
  {% else %}
    <title>{{ page_title }} | {{ shop.name }}</title>
  {% endif %}

  {% if page_description %}
  <meta name="description" content="{{ page_description }}" />
  {% endif %}

  <link rel="canonical" href="{{ canonical_url }}" />

  {% include 'advanced-settings' with 'variables' %}
  {% include 'snippet-fb-open-graph-tags' %}
  {% include 'snippet-webfont' %}

  {{ content_for_header }}

  {{ 'beans.css' | asset_url | stylesheet_tag }}

  <script>window.beans_cjs_id = '{{ customer.id }}'; window.beans_cjs_email = '{{ customer.email }}';</script> 


  {{ 'theme.scss.css' | asset_url | stylesheet_tag }}
  {{ 'custom.scss.css' | asset_url | stylesheet_tag }}
  {{ 'swiper.css' | asset_url | stylesheet_tag }}
  {% include 'advanced-settings' with 'styles' %}
  {% include 'custom-styles' %}

  <!--[if IE]>
    <style>
      .main-header .currency-selector select {
        width: 80px;
        padding-right: 10px;
      }
    </style>
  <![endif]-->

  <!-- >= IE 11 -->
  <style>
    @media screen and (-ms-high-contrast: active), (-ms-high-contrast: none) {
      .main-header .currency-selector select {
        width: 80px;
        padding-right: 10px;
      }
    }
  </style>

  {{ '//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js' | script_tag }}
  {{ '//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js' | script_tag }}
  {{ 'api.jquery.js' | shopify_asset_url | script_tag }}
  {{ 'option_selection.js' | shopify_asset_url | script_tag }}
  {{ 'swiper.min.js' | asset_url | script_tag }}
  	<!-- This site is converting visitors into subscribers and customers with OptinMonster - https://optinmonster.com --><script>var om49782_43498,om49782_43498_poll=function(){var r=0;return function(n,l){clearInterval(r),r=setInterval(n,l)}}();!function(e,t,n){if(e.getElementById(n)){om49782_43498_poll(function(){if(window['om_loaded']){if(!om49782_43498){om49782_43498=new OptinMonsterApp();return om49782_43498.init({"a":43498,"staging":0,"dev":0,"beta":0});}}},25);return;}var d=false,o=e.createElement(t);o.id=n,o.src="https://a.optmstr.com/app/js/api.min.js",o.async=true,o.onload=o.onreadystatechange=function(){if(!d){if(!this.readyState||this.readyState==="loaded"||this.readyState==="complete"){try{d=om_loaded=true;om49782_43498=new OptinMonsterApp();om49782_43498.init({"a":43498,"staging":0,"dev":0,"beta":0});o.onload=o.onreadystatechange=null;}catch(t){}}}};(document.getElementsByTagName("head")[0]||document.documentElement).appendChild(o)}(document,"script","omapi-script");</script><!-- / OptinMonster -->
	<script type="text/javascript">
        var om_load_webfont = false;
    </script>
	
  
  {% if template contains 'customers' %}
    {{ 'shopify_common.js' | shopify_asset_url | script_tag }}
    {{ 'customer_area.js'  | shopify_asset_url | script_tag }}
  {% endif %}

  <script>
    var twitterID = '{{ settings.twitter_username }}',
    general_external_links_enabled = true,
    general_scroll_to_active_item = true,
    shop_url = '{{ shop.url }}',
    shop_money_format = '{{ shop.money_format | strip_html | replace: "'", '"' | strip }}',
    currency_switcher_enabled = {{ settings.show_multiple_currencies }},
    popup_config = {
      enabled               : {{ settings.popup--enabled }},
      test_mode             : {{ settings.popup--test-mode }},
      newsletter_enabled    : {{ settings.popup--newsletter--enabled }},
      social_icons_enabled  : {{ settings.popup--social-icons--enabled }},
      show_image_enabled    : '{{ settings.popup--image.size }}',
      image_link            : "{{ settings.popup--image-url }}",
      seconds_until         : {{ settings.popup--seconds-range }},

      {% comment %} So it doesnt get picked up by og:image {% endcomment %}
      {% if settings.popup--image %}
      show_image_url        : '{{ settings.popup--image | img_url: "1200x" | img_tag: settings.popup--image.alt | default: "" }}',
      {% endif %}

      page_content          : '{{ settings.popup--page-content }}',
      days_until            : '{{ settings.popup--days-range }}',
      storage_key           : 'troop_popup_storage'
    };

    popup_config.show_image_enabled = popup_config.show_image_enabled > 0 ?
                                      true :
                                      false;

    // newer naming convention
    var theme = {};
    theme.settings = {};
    theme.translation = {}; // refactor to pluralised
    theme.translations = {};
    theme.classes = {};
    theme.utils = {};
    theme.templates = {};
    theme.partials = {};
    theme.shop_currency = '{{ shop.currency }}';

    theme.translation.newsletter_success_message = '{{ "layout.footer.newsletter_success_message" | t }}';
    theme.translation.newsletter_email_blank = '{{ "layout.footer.newsletter_email_blank" | t }}';
    theme.translations.general_search_see_all_results = '{{ "general.search.see_all_results" | t }}';
    theme.preload_image = '{{ "popup.jpg" | asset_url }}';

  </script>

  {% if settings.show_multiple_currencies %}
    {% include 'snippet-currencies' %}
  {% endif %}

  {{ 'theme.js' | asset_url | script_tag }}

<!-- TrustBox script -->
<script type="text/javascript" src="//widget.trustpilot.com/bootstrap/v5/tp.widget.bootstrap.min.js" async></script>
<!-- End Trustbox script -->
</head>

<body class="page-{{ page_title | handle }} {% if customer %}customer-logged-in {% endif %}template-{{ template | split: '.' | first | remove: '/' | remove: '_' }}">

  {% include 'icon-sprite.svg' %}
  {%comment%}{% include 'facebook-chat-plugin' %}{%endcomment%}
  <div
    class="off-canvas--viewport"
    data-js-class="FrameworkOffCanvas"
    data-off-canvas--state="closed"
  >
    <div class="off-canvas--overlay"></div>
    <div class="off-canvas--close">{% include 'framework--icon', icon: 'cross' %}</div>

    <div class="off-canvas--left-sidebar">
      {% include 'partial--mobile-nav' %}
    </div>

    <div class="off-canvas--main-content">
      {% section 'custom-header' %}

      {% comment %}<a href="#" class="manual-optin-trigger" data-optin-slug="{{ settings.theme_optin }}">Click Me!</a>	{% endcomment %}  

      
      <section class="main-content">
        {{ content_for_layout }}
      </section>
      
      {% section 'custom-customers-slider' %}
      
      {% section 'custom-promo' %}
      
      {% section 'featured-promo' %}
      
      {% section 'custom-question' %}
      
      {% section 'social-feeds' %}
      
      {% section 'custom-footer' %}

{% comment %}{% section 'footer' %}{% endcomment %}

      {% unless template contains 'password' %}
        <button class="popup-modal-close">&#x2715;</button>
        <div class="popup-modal-mask"></div>
      {% endunless %}

      <!--<style> iframe#admin_bar_iframe { display: none !important; } </style>-->
    </div>

    <div class="off-canvas--right-sidebar">
      <div class="off-canvas--close">{% include 'framework--icon', icon: 'cross' %}</div>
    </div>

  </div>

  <script>
    document.addEventListener("DOMContentLoaded",
                              function() {
      var div, n,
          v = document.getElementsByClassName("youtube-player");
      for (n = 0; n < v.length; n++) {
        div = document.createElement("div");
        div.setAttribute("data-id", v[n].dataset.id);
        div.innerHTML = labnolThumb(v[n].dataset.id);
        div.onclick = labnolIframe;
        v[n].appendChild(div);
      }
    });

    function labnolThumb(id) {
      var thumb = '<img src="https://i.ytimg.com/vi/ID/hqdefault.jpg">',
          play = '<div class="play"></div>';
      return thumb.replace("ID", id) + play;
    }

    function labnolIframe() {
      var iframe = document.createElement("iframe");
      var embed = "https://www.youtube.com/embed/ID?rel=0&amp;controls=0&amp;showinfo=0";
      iframe.setAttribute("src", embed.replace("ID", this.dataset.id));
      iframe.setAttribute("frameborder", "0");
      iframe.setAttribute("allowfullscreen", "1");
      $(this).hide();
      this.parentNode.append(iframe);
      //this.parentNode.replaceChild(iframe, this);
    }
    document.addEventListener("DOMContentLoaded", function(event) {
    
    $('.accordion').on('click', function(){
      $('.accordion').removeClass('active');
      panel = $(this).next();
      if (panel.hasClass('active')){
        panel.removeClass('active');
      } else {
        $(this).addClass('active');
        $('.accordion-panel').removeClass('active')
        panel.addClass('active');
          $('html, body').animate({
            scrollTop: $('.accordion.active').offset().top - 100
          }, 300);
      }
    });

  });

  </script>
  
<!-- BEEKETINGSCRIPT CODE START -->{% include 'bk-tracking' %}<!-- BEEKETINGSCRIPT CODE END --></body>
</html>
